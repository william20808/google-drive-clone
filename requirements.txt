Django>=5.0,<6.0
Pillow>=10.0.0
gunicorn>=21.2.0
cryptography>=41.0.0

# Note: For production deployments (PostgreSQL & Cloud Object Storage like S3),
# install requirements-prod.txt (pip install -r requirements-prod.txt)
